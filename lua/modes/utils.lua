local M = {}

M.map = function()
    -- TODO create a map function that will check if there is existing keymap
    -- and if present cache it so that it can be reverted later
end

M.unmap = function()
    -- TODO create a unmap function that revert existing keymap if present in cache
end

return M
